{
  "name": "overhear-backend",
  "version": "1.0.0",
  "description": "Local proxy server that generates travel guides via the Anthropic API without exposing the API key to the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
